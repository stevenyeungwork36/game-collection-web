# Copy to .env (repo root) or frontend/.env.local — do not commit those files.
#
# Cloudflare Pages + Functions proxy: leave empty so the browser uses same-origin /api/...
# (Pages Function proxies to BACKEND_URL set in Cloudflare dashboard, not here.)
#
# Direct Worker URL in the browser (optional):
# VITE_API_BASE_URL=https://game-collection-backend.sy-dev.workers.dev
VITE_API_BASE_URL=
#
# Local Vite only: where to proxy /api when VITE_API_BASE_URL is empty
# VITE_DEV_PROXY_API_TARGET=https://game-collection-backend.sy-dev.workers.dev
